//2. Find the maximum number from two numbers. 

#include<stdio.h>
#include<stdlib.h>

void main(int argc,char *argv[])
{
	int a = atoi(argv[1]);
	int b = atoi(argv[2]);
	
	if (a>b)
	{
		printf("A Is the Maximum Number ");
	}
	else
	printf("B Is the Maximum Number ");	
}
